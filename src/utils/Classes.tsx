import React from "react";

export const Emphasis = ({ children }) => (
  <span className="emphasis">{children}</span>
);
export const Danger = ({ children }) => (
  <span className="emphasis danger">{children}</span>
);

export const Inform = ({ children }) => (
  <span className="inform">{children}</span>
);
export const Small = ({ children }) => (
  <span className="small">{children}</span>
);
